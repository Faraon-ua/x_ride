<h1><a href="index.html"><img src="images/logo.png" alt=""></a></h1>

<div class="descr">The club for all<br>
  categories of riders
</div>

<div id="slideshow">
  <ul class="bjqs">
    <% @slides.each do |slide| %>
        <%= render "slide", :slide => slide %>
    <% end %>
  </ul>
</div>